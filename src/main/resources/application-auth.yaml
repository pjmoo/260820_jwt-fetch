app:
  auth:
    encoding-id: argon2
    jwt:
      secret-key: ${JWT_SECRET_KEY}
      access-token-expiry: 5m
---
spring:
  config:
    activate:
      on-profile: dev
  security:
    user:
      name: user
      password: test